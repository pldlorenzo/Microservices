server:
  port: 8080
spring:
  datasource:
    url: jdbc:h2:mem:accountsdb
    driverClassName: org.h2.Driver
    username: pdlorenzo
    password: ''
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  config:
    import:
      - "application_qa.yml"
      - "application_prod.yml"
  profiles:
    active:
      - "qa"

build:
  version: "3.0"

accounts:
  message: "Welcome to PDL Bank accounts related local APIs "
  contactDetails:
    name: "Paulo Lorenzo - Developer"
    email: "paulo@pdlorenzo.com"
  onCallSupport:
    - (555) 555-1234
    - (555) 530-1222